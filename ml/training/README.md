# Training placeholders

Scripts and configs will land here. For now:

1. Prefer open baselines (detection) before custom weight heads.
2. Map geometry proxies → kg with species-specific regression **after** holdout eval.
3. Log calibration version with every training run.

No training code claims production accuracy yet.
